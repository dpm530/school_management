<!DOCTYPE html>
<html lang="en" dir="ltr">
   <head>
      <meta charset="utf-8">
      <title></title>
   </head>
   <body>
      <div class="row">
         <div class="col-3"></div>
         <div class="col-6 text-center">
            <h1>Welcome, <%= @teacher.first_name.capitalize %> <%= @teacher.last_name.capitalize %></h1>
            <a href="/courses" class="btn btn-primary">Courses</a>
            <a href="/students_contact_info" class="btn btn-primary">Contact Information</a>
            <a href="/logout" data-method="delete" class="btn btn-primary">Logout</a>
         </div>
         <div class="col-3"></div>
      </div>
      <br>
      <div class="row">
         <div class="col-3"></div>
         <div class="col-6">
            <div class="container">
               <h1>Courses:</h1>
               <table class="table table-bordered">
                  <thead>
                     <tr>
                        <th>Course</th>
                        <th>Subject</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                     </tr>
                  </thead>
                  <tbody>
                     <% if @teacher_course %>
                        <% @teacher_course.each do |course| %>
                           <tr>
                              <td><%= course.name.capitalize %></td>
                              <td><%= course.subject.capitalize %></td>
                              <td><%= course.start_date %></td>
                              <td><%= course.end_date %></td>
                           </tr>
                        <% end %>
                     <% end %>
                  </tbody>
               </table>
            </div>
         </div>
         <div class="col-3"></div>
      </div>
   </body>
</html>
