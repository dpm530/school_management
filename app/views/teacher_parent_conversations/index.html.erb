<!DOCTYPE html>
<html lang="en" dir="ltr">
   <head>
      <meta charset="utf-8">
      <title></title>
   </head>
   <body>

      <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom mb-4">
         <% if current_administrator %>
            <a class="navbar-brand" href="/administrators">Open-School</a>
         <% end %>
         <% if current_teacher %>
            <a class="navbar-brand" href="/teachers">Open-School</a>
         <% end %>
         <% if current_parent %>
            <a class="navbar-brand" href="/parents">Open-School</a>
         <% end %>

         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
         </button>

         <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
               <li class="nav-item active">
                  <a class="nav-link" href="/administrators">Home</a>
               </li>
               <li class="nav-item active">
                  <a class="nav-link" href="#"><i class="fas fa-bell"></i></a>
               </li>
               <li class="nav-item border-right pr-2">
                  <a class="nav-link" href="#"><i class="fas fa-envelope"></i></a>
               </li>
               <% if current_administrator %>
                  <li class="nav-item dropdown pl-1">
                     <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                         <%=current_administrator.login.capitalize%> <i class="fas fa-user-circle"></i>
                     </a>
                     <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/administrators/<%= current_administrator.id %>">Profile</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/sessions/logout" data-method="delete">Logout</a>
                     </div>
                  </li>
               <% end %>
               <% if current_guest %>
                  <li class="nav-item dropdown pl-1">
                     <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Guest <i class="fas fa-user-circle"></i>
                     </a>
                     <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="">Profile</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/sessions/logout" data-method="delete">Logout</a>
                     </div>
                  </li>
               <% end %>
               <% if current_teacher %>
                  <li class="nav-item dropdown pl-1">
                     <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= current_teacher.first_name.capitalize %> <%= current_teacher.last_name.capitalize %> <i class="fas fa-user-circle"></i>
                     </a>
                     <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="">Profile</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/sessions/logout" data-method="delete">Logout</a>
                     </div>
                  </li>
               <% end %>
               <% if current_parent %>
                  <li class="nav-item dropdown pl-1">
                     <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= current_parent.first_name.capitalize %> <%= current_parent.last_name.capitalize %> <i class="fas fa-user-circle"></i>
                     </a>
                     <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="">Profile</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/sessions/logout" data-method="delete">Logout</a>
                     </div>
                  </li>
               <% end %>
            </ul>
         </div>
      </nav>

      <div class="container">
         <h1> New Conversation: </h1>

            <% if current_teacher %>
               <form class="" action="/teacher_parent_conversations/teacher/<%= current_teacher.id %>" method="post">
                  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />

                  <div class="form-group">
                     <label for="parent">Select list:</label>
                     <select class="form-control" id="parent" name="parent">
                        <option></option>
                        <% @parents.each do |parent| %>
                           <option value="<%= parent.id %>"><%= parent.first_name.capitalize %> <%= parent.last_name.capitalize %></option>
                        <% end %>
                     </select>
                     <button type="submit" name="button" class="btn btn-primary">Message</button>
                  </div>

               </form>
            <% end %>

            <% if current_parent %>
               <form class="" action="/teacher_parent_conversations/parent/<%= current_parent.id %>" method="post">
                  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />

                  <div class="form-group">
                     <label for="teacher">Select list:</label>
                     <select class="form-control" id="teacher" name="teacher">
                        <option></option>
                        <% @teachers.each do |teacher| %>
                           <option value="<%= teacher.id %>"><%= teacher.first_name.capitalize %> <%= teacher.last_name.capitalize %></option>
                        <% end %>
                     </select>
                     <button type="submit" name="button" class="btn btn-primary pt-2">Message</button>
                  </div>

               </form>
            <% end %>


      </div>

      <hr>

      <div class="container">
         <div class="row">
            <div class="col-md-6">
               <h1>Inbox:</h1>

               <% @inbox.each do |item| %>
                  <% if current_parent%>
                     <div class="card bg-light">
                        <div class="card-body">
                           <a href="/teacher_parent_conversations/<%= item.id %>"><%= item.teacher.first_name.capitalize %> <%= item.teacher.last_name.capitalize %></a>
                        </div>
                     </div>
                  <% end %>
                  <% if current_teacher %>
                     <div class="card bg-light">
                        <div class="card-body">
                           <a href="/teacher_parent_conversations/<%= item.id %>"><%= item.parent.first_name.capitalize %> <%= item.parent.last_name.capitalize %></a>
                        </div>
                     </div>
                  <% end %>
                  <br>
               <% end %>
            </div>
         </div>

      </div>

   </body>
</html>
