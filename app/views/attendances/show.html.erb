<!DOCTYPE html>
<html lang="en" dir="ltr">
   <head>
      <meta charset="utf-8">
      <title></title>
   </head>
   <body>
      <div class="container">
         <div class="row">
            <div class="col-md-12 text-center">
               <h1>Attendance Show</h1>
               <h2>Course: <%= @course.name.capitalize %></h2>
            </div>
         </div>
         <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
               <%= form_tag attendances_path do %>
                  <% @classroom.each do |student|%>
                     <% fields_for 'students[]', student do |s| %>

                        <div class="field">
                           <%= s.label 'present', 'Attendance' %><br>
                           <%= s.text_field 'present' %>
                        </div>

                        <div class="field">
                           <%= s.label 'date', 'Date'%><br>
                           <%= s.date_field 'date' %>
                        </div>
                        <hr>
                        
                     <% end %>
                  <% end %>
                  <%= submit_tag "Submit" %>
               <% end %>
            </div>
            <div class="col-md-3"></div>
         </div>
      </div>
   </body>
</html>
